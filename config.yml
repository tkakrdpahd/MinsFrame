# config.yml

# 프로젝트 정보
project:
  name: "mins-frame"
  version: "1.0.0"
  description: "Mins Frame"

# 프로젝트 설정
frontend:
  port: 3000
  host: 0.0.0.0
  env:
    - .env

backend:
  port: 5001
  host: 0.0.0.0
  env:
    - .env
